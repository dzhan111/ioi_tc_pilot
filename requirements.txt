# CPU-only PyTorch — install separately:
#   pip install torch==2.2.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.2.0

# Core ML / interpretability
transformer-lens==1.11.0
einops==0.7.0
jaxtyping==0.2.28

# Data
numpy==1.26.2
scipy==1.12.0

# HuggingFace (transcoder download + tokeniser)
huggingface-hub>=0.34.0,<1.0
datasets==2.16.0

# Utilities
tqdm==4.66.1
matplotlib==3.8.2
joblib==1.3.2
plotly==5.19.0

# Dev / testing
pytest==7.4.4
setuptools>=68.0

# NOTE: wandb is NOT required. transformer_lens 1.11.0 imports it in train.py;
# the vendored copy of train.py has the import made optional.
